# config/default.yaml — FULL FILE (OVERWRITE)
slime:
  steps: 400
  max_slope: 0.012
  min_velocity: 0.6

optimizer: hybrid

# HYBRID TOGGLE
use_hybrid: true

# VORTEX & TERMITE
use_vortex: true
use_termite: true

# HVAC PULSE
pulse_freq: 7.83
